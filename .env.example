# ─── Imágenes publicadas en GHCR ─────────────────────────────────────────────
# Actualizá estos valores cuando salga un nuevo build en CI.
# Para que el proxy funcione, el frontend debe haberse compilado con
# VITE_ORDER_API_BASE vacío (valor recomendado en vars de GitHub Actions).

FRONTEND_IMAGE=ghcr.io/optimizador-de-envios/optimizador-envios-frontend:sha-e1b59a2
BACKEND_IMAGE=ghcr.io/optimizador-de-envios/optimizador-envios-backend:sha-4032e2b

# ─── Backend ─────────────────────────────────────────────────────────────────
# API key de OpenRouteService. Obtené la tuya en:
# https://openrouteservice.org/dev/#/signup
ORS_API_KEY=YOUR_API_KEY_HERE
